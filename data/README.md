# Data

The SQL scripts in this repository expect a relational database containing the following tables:

- `transactions`
- `products`
- `customers`
- `markets`
- `date`

The original project notes do not include the underlying dataset, so raw data is intentionally not included in this repository package.

Before running the queries, load the source data into your database and verify the table/column names described in the main README.
